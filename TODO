### INITIAL SETUP

* Create welcome screen
* Create screen that quizzes user to determine what meditation would be best for them
    * Write algorithm to determine for user
    * Present to user results of algorithm
* Create username and password screen
* Should this be the home route if the user isn't logged in? A non-logged in user sees the splash screen; logged-in user sees their tasks


### FRONT-END

* Figure out color schema - DONE
    * Light orange as primary color? - DONE
* Just using Bootstrap with custom styles? - DONE
* Fix the navbar for user login and settings
* Hook tasks into main page

### BACK-END

* Set up SQL tables - DONE
* Implement user accounts
* Implement tasks